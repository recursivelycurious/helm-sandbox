apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-sandbox.fullname" . }}
  labels:
    app: {{ template "helm-sandbox.name" . }}
    chart: {{ template "helm-sandbox.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-sandbox.name" . }}
    release: {{ .Release.Name }}
